-- ============================================================
--  Міграція 032: Керування розсилкою нагадувань клієнтам/тренерам.
--
--  Раніше чекбокси «Нагадування клієнтам» / «Нагадування тренерам»
--  у налаштуваннях адміністратора зберігались лише в localStorage
--  браузера й ні на що не впливали. Тепер це реальні прапорці клубу:
--  вимикають надсилання сповіщень категорії 'training' (підтвердження
--  запису, перенесення/скасування заняття, призначення тренера)
--  відповідно клієнтам або тренерам — див. server/utils/notify.js.
-- ============================================================

ALTER TABLE public.club_settings
  ADD COLUMN IF NOT EXISTS remind_clients  boolean NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS remind_trainers boolean NOT NULL DEFAULT true;
